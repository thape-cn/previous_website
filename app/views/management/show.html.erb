<% provide(:title, "管理团队") %>

<div class="container personal">
  <div class="row">
    <div class="col-md-6">
      <%= image_tag(@person.photo.url, class: "personal-img") %>
    </div>
    <div class="col-md-6">
      <h4><%= @person.name %></h4>
      <hr>
      <h5><%= simple_format @person.title %></h5>
      <%= raw @person.introduce %>
    </div>
  </div>


</div>
